const MessageList = (messages) => `
    <div class="message-list">
        ${messages.map((message) => {
            // тут должен быть шаблон сообщения
        })}
    </div>
`;

export default MessageList;
